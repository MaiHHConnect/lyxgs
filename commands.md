# 命令映射

所有命令入口都必须先执行 `ensure_user(openclaw_user_id)`，并遵守 `security.md`。

## 领养小怪兽

- 意图：首次领养。
- 消耗：100 能量。
- 动作：`adopt_one(pool_type='first')`。
- 限制：能量不足拒绝；卡槽满拒绝；冻结中拒绝。
- 输出：先展示蛋图；若已有卡牌图存在则直接展示卡牌。

## 抽一只小怪兽 / 单抽

- 消耗：300 能量。
- 动作：`adopt_one(pool_type='normal')`。
- 保底：10 抽 SR+，50 抽 SSR+，200 抽 UR+；MR 不保底。
- 限制：最多 3 卡槽；满槽只能融合。

## 十连领养

- 消耗：2700 能量。
- 动作：`adopt_ten(pool_type='normal')`。
- 限制：若可用卡槽不足，只允许写入空槽数内的怪兽；不能超过 3 槽。
- 输出：按实际写入数量展示蛋列表和剩余能量。

## 指定元素领养

- 消耗：500 能量。
- 参数：元素必须是 `火、水、雷、草、光、暗、土、风、机械、梦境`。
- 动作：`adopt_one(pool_type='element', element=...)`。
- 限制：只筛选候选池，不修改图鉴概率配置。

## 高级领养池

- 消耗：800 能量。
- 动作：`adopt_one(pool_type='advanced')`。
- 限制：高级池概率只在运行时计算，不修改 `rarity_config`。

## 限定池

- 消耗：1200 能量。
- 动作：`adopt_one(pool_type='limited')`。
- 限制：活动规则只在运行时计算，不修改图鉴主数据。

## 查看我的小怪兽

- 动作：`show_my_monsters()`。
- 权限：只读当前用户 `user_monsters`。
- 输出：槽位、怪兽名、稀有度、等级、能量、图片路径。

## 喂养小怪兽

- 参数：槽位 1/2/3，喂养能量。
- 动作：`feed_monster(slot_index, feed_energy)`。
- 限制：只允许喂养当前用户自己的怪兽；账户能量不能为负。

## 融合小怪兽

- 参数：继承怪兽、消耗怪兽、融合后归属。
- 动作：`start_fusion()`。
- 限制：必须明确谁继承、谁消耗；他人融合必须双方确认；不能修改图鉴；不能删除图片。

## 确认融合

- 动作：`confirm_fusion(fusion_id)`。
- 限制：只能确认与当前用户相关的融合请求。

## 查看排行榜

- 动作：`show_leaderboard(board_type)`。
- 类型：能量榜、收藏榜。
- 权限：只读聚合，不导出用户全量数据。

## 查看能量

- 动作：`show_energy()`。
- 权限：只读当前用户。

## 重新孵化高清图

- 动作：`request_regenerate_card(slot_index, overwrite=true)`。
- 默认禁用覆盖。
- 必须用户明确说“重新孵化高清图/覆盖生成”。
- 需要维护者或显式权限确认。
- 失败时保留旧图。
